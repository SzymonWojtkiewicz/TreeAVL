Klasa tree to klasa z ktorej tworzone jest drzewo AVL 
metody musza byc wywolywane na obiekcie klasy
wskaznik na korzen zawsze istnieje w obiekcie, wskazuje na nullptr jeli nie ma danych, nie wymaga inicjalizacji (od razu do pustego drzewa mozna uzyc metody addData())
metoda wypisz stworzy wizualizacje drzewa w pliku o nazwie argumentu w folderze programu
najlepiej dziala dla liczb maksymalnie 3 znakowych